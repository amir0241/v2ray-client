package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Professional Polish Theme Colors
val CyberCyan = Color(0xFF005FB0)       // Primary action brand blue (#005FB0)
val DarkCosmicSlate = Color(0xFFF7F9FC)  // Light premium slate background (#F7F9FC)
val TechCardBg = Color(0xFFFFFFFF)       // Crisp white container cards (#FFFFFF)
val EmeraldActive = Color(0xFF10B981)    // Active / connected green (#10B981)
val SafeAmber = Color(0xFFD97706)       // Warnings and ping latency amber (#D97706)
val DangerRed = Color(0xFFEF4444)       // Core alerts and timeout status red (#EF4444)

val IceWhite = Color(0xFF1B1B1F)        // Primary contrastive body text (#1B1B1F)
val PaleBlueGrey = Color(0xFF6B7280)    // Subtitle annotations / secondary text

// Secondary accents and backgrounds
val SolarPurple = Color(0xFF3B82F6)      // Secondary active blue
val DeepIndigoBg = Color(0xFFD1E4FF)     // Active main highlight tint container (#D1E4FF)
val BorderSlate = Color(0xFFE1E2E6)      // Standard thin neutral gray card borders (#E1E2E6)

